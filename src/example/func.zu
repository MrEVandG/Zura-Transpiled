# Make a function that adds to numbers together
fn fib(n: i32) -> i32 {
    if(n <= 1) ret n;
    ret fib(n - 1) + fib(n - 2);
}

fn main() -> !void {
    auto fib = fib(10);
    info("fib(10) = %d", .{fib}); ~
    ret 0
}
